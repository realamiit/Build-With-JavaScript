// for store are the input and show on screen

let firstname = prompt("Enter your First name : ");
let lastname = prompt("Enter your Last name : ");
let msg = "Welcome" + firstname + lastname + "!";
alert(msg);

// This JavaScript code asks the user to enter their first and last name using prompt().
//  It stores them in variables firstname and lastname.
//  Then it creates a welcome message by combining both names into one string.
//  Finally, it display the message using alert(), greeting the user with their full name on the screen.
