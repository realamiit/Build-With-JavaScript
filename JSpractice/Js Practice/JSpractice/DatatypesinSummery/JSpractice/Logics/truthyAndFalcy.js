// Truthy and Falcy :There is everything in JS true or false (in Bollean context).
    // this does't mean there value is itself is false or true , but they are treated as a false or true if taken is bollean context.

    // Falcy value => false ,0 ,-0 on(Biglnt Value), "" (empty String) , null, undefened ,NaN

    // Truthy Value => everythings else

        if(""){
            console.log("it  is tru value")
        }else 
            console.log("it is false value")


 let string = "";
            if(string){
            console.log("it  is tru value")
        }else 
            console.log("String is empty")
