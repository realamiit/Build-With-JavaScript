// let msg = ("    AmitGupta    ");

let password = prompt("Set your password");
let newpassword = password.trim();
console.log(password);   //if we go wit password w'll got same pass,
                      //  if w'll go with new pass will recive trimd spapcs pass